

// Ejercicio 2: Imprimir los Números Pares del 1 al 20


for (let i = 1; i <= 20; i++) { // Se inicializa la variable en 1 y hacemos que el bucle pare en el numero 20

    if (i % 2 === 0) { // Con un if hacemos la condición de que si el resto de la división entre 2, es igual a 0, es par entonces imprimimos en consola. 
        console.log(i)
    }
}