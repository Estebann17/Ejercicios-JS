

function revertirArray(array) { // 

    array.reverse();

    return array;
}

console.log(revertirArray([1, 2, 3, 4, 5]));

// Bueno lo que hice acá fue utilizar el método reverse() para que me devuelva el array invertido. 